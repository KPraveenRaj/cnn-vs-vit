# Frozen after the completed Caltech-256 matrix (2026-08-22) — these are the
# exact versions that produced every number in results/tables/.
#
# Only the packages this project imports are pinned. A bare `pip freeze` of the
# conda env would add ~200 unrelated transitive packages and make the file
# useless as a description of what the study actually depends on.
#
# torch here is the CUDA 12.1 build; install it from the PyTorch index, not
# PyPI, or you will get a CPU-only wheel:
#   pip install torch==2.5.1 torchvision==0.20.1 \
#       --index-url https://download.pytorch.org/whl/cu121

torch==2.5.1+cu121
torchvision==0.20.1+cu121
timm==1.0.27
numpy==2.2.6
pandas==2.3.3
pyarrow==24.0.0
scikit-learn==1.7.2
scipy==1.15.3
matplotlib==3.10.9
PyYAML==6.0.3
pillow==12.2.0
jupyter==1.1.1
ipykernel==7.3.0
python-pptx==1.0.2
python-docx==1.2.0
