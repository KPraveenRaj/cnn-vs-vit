# Core stack: PyTorch + timm, all open-source.
# DRAFT list — exact versions will be frozen (pip freeze) after the first
# full training run of the experiment matrix.
torch
torchvision
timm
numpy
pandas
pyarrow
scikit-learn
scipy          # explicit: gaussian_filter in src/eval/corruptions.py (already required by scikit-learn)
matplotlib
pyyaml
pillow
jupyter
ipykernel
python-pptx    # report/deck generation
python-docx    # report generation
